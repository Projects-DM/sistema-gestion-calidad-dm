# S23.7 — ACTUAL PATCH EXECUTION DESIGN (REPO-LEVEL CONTROLLED IMPACT)

## IMPORTANTE

- ESTE DOCUMENTO DEFINE CAMBIOS REALES A NIVEL DE REPOSITORIO (PRE-DIFF STAGE)
- NO EJECUTAR SIN VALIDAR S23.3 (FINAL GATE)
- NO SALIR DEL SCOPE SPRINT 23
- NO MODIFICAR CORE ARCHITECTURE
- NO ALTERAR PROVIDER FACTORY DESIGN

---

## STATUS
Pre-Execution Design Layer (Repository Impact Specification)

---

## CONTEXTO

Entrada:
- S23.1 → Arquitectura
- S23.2 → Plan
- S23.3 → Control gates
- S23.4 → Orquestación
- S23.5 → Patch specification
- S23.6 → Implementation translation

Salida:
👉 DEFINICIÓN EXACTA DE IMPACTO EN REPOSITORIO (PRE-DIFF STAGE)

---

## FASE 1 — REPOSITORY IMPACT MODEL

### SaaS Layer Impact

#### FILE: dynamicService.ts

Cambios permitidos:

1) **Event Hook Injection**
- En:
  - submitFormResponse()
  - verifyFormResponse()
- Agregar:
  - pre-audit hook (no side-effects)
  - event normalization layer
  - mapping to FORM_* taxonomy

2) **Event Normalization Function**

`normalizeBusinessEvent(event)`

Responsabilidad:
- transformar:
  - `create` → `FORM_CREATED`
  - `verify` → `FORM_VERIFIED`
- construir payload determinista
- fuente de `eventId` proveniente del `sgc_audit_logs`

Reglas:
- NO modificar estructura de tabla
- NO alterar schema

---

### FASE 2 — RUNTIME IMPACT MODEL

#### FILE: PersistenceExecutionRouter.ts

Cambios permitidos:
1) Injection Point (CRÍTICO)
- Antes de:
  - `auditRecorder.recordExecutionStarted(...)`
- Insertar:
  - `businessEventTranslationHook()`

2) Translation Hook Responsibility
- recibe normalized SaaS event
- mapea a runtime-compatible audit envelope
- preserva `correlationId`

#### FILE: RuntimeExecutionAuditRecorder.ts

Cambios permitidos:
- aceptar metadata extendida SOLO para mapping
- NO modificar core audit logic
- NO alterar auditId generation
- NO alterar providerId logic

---

## FASE 3 — EXECUTION DIFF MODEL (conceptual)

PATCH 1 — SaaS Hook Layer
- `dynamicService.submitFormResponse`
- `dynamicService.verifyFormResponse`

→ ADD:
- event emission hook
- normalization function call

PATCH 2 — Translation Layer
- NUEVO concepto (NO core rewrite):
  - `BusinessEventTranslationLayer`
- Responsabilidad:
  - convertir SaaS → Runtime audit format

PATCH 3 — Router Injection
- `PersistenceExecutionRouter`:
  - BEFORE auditRecorder call:
  - apply translation hook

PATCH 4 — Audit Compatibility
- `RuntimeExecutionAuditRecorder`:
  - accept enriched metadata
  - preserve internal audit generation

---

## FASE 4 — FILE IMPACT SUMMARY

SaaS
- `dynamicService.ts` → MODIFY (hooks)
- NO schema changes

Runtime
- `PersistenceExecutionRouter.ts` → MODIFY (injection point)
- `RuntimeExecutionAuditRecorder.ts` → MINOR ADAPTATION (metadata acceptance)

---

## FASE 5 — EXECUTION SAFETY MODEL

HARD RULES
- NO provider factory modification
- NO analytics engine modification
- NO scoring engine modification
- NO dispatch system usage

FAILURE CONDITIONS
- Execution MUST STOP if:
  - EAV raw leaks into payload
  - event mapping is not deterministic
  - router is bypassed
  - correlationId is missing
  - audit chain is broken

---

## FASE 6 — FINAL EXECUTION FLOW

SaaS Event
  ↓
Normalization Layer
  ↓
BusinessEventTranslationLayer
  ↓
PersistenceExecutionRouter
  ↓
RuntimeExecutionAuditRecorder
  ↓
RuntimeExecutionAuditRegistry
  ↓
Analytics Engine
  ↓
Scoring Engine

---

## FASE 7 — EXECUTION READINESS

READY STATUS

| Layer | Status |
|---|---|
| SaaS hooks | READY |
| Translation layer | READY |
| Router injection | READY |
| Audit core | READY (unchanged) |
| Analytics | READY |
| Scoring | PARTIAL |

---

## FINAL STATEMENT

Este documento marca el límite entre:
- diseño
- ejecución controlada real

No ejecuta cambios, pero define exactamente el diff que será aplicado en Sprint 23 (solo después del gate S23.3).

